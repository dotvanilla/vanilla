Module logicalTest

    Sub Main()
        Dim displayWidth!
        Dim x, y, r As Double
        Dim displayHeight!

        If (displayWidth - (x + r) < 0) OrElse (x - r < 0) OrElse (displayHeight - (y + r) < 0) OrElse (y - r < 0) Then

        End If
    End Sub
End Module
